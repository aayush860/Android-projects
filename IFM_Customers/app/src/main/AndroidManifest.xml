<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.anew.ifm">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
<uses-permission android:name="android.permission.READ_CONTACTS"></uses-permission>


    <application
        android:allowBackup="true"
        android:icon="@drawable/loggoo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".page0"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".logout"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name=".refff" />
        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name=".natlaogin"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name=".googleinterface"></activity>
        <activity android:name=".abtus" />
        <activity android:name=".contactusss" />
        <activity android:name=".exitc" />


        <meta-data android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id"/>

        <activity android:name="com.facebook.FacebookActivity"
            android:configChanges=
                "keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>


        <activity android:name=".page1"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".clos" />
        <activity android:name=".offers" />
        <activity android:name=".manual_address"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".regActivity"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".listview1"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".ImageViewerr"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".confirmc"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".totalc"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".MapsActivity" />
        <activity android:name=".orderxx" />
        <activity android:name=".lastcl" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyB7-GWXFvSmPS2n03-EaOB4_BtHLFt4m1I" />


    </application>

</manifest>